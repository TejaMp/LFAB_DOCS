FOR ALL THE DATA GUARD environment, THE STEPS TO PERFORM
CHECK SYNC
select max(sequence#) from v$archived_log where applied='YES';

set lines 300
set long 999999
select * from v$managed_standby;
archive log list;
alter system switch logfile;
alter system switch logfile;
1) start maintenance
2) stop zipping job in primary database - 
check the offredolog size = df -g
compress archive redo log ########
3) stop delarchive logs jobs in standby database and compr _redo in primary database
4) disable configuration in (standby database) - dgmgrl /
DGMGRL> show configuration;

Configuration - dg_iomprd

  Protection Mode: MaxPerformance
  Members:
  iomprd - Primary database
    iomsmp - Physical standby database

Fast-Start Failover:  Disabled

Configuration Status:
SUCCESS   (status updated 35 seconds ago)

DGMGRL>
DGMGRL> disable configuration
Disabled.

5) alter system set dg_broker_Start=false; (In standby database)
6) ALTER DATABASE RECOVER MANAGED STANDBY DATABASE CANCEL; (In standby database) - NOT REQUIRED
7) stop the listener in standby
8) Shutdown the standby database

9) "startup mount" the standby database
10) ALTER SYSTEM SET DG_BROKER_START= TRUE; (In standby Database)
11) alter database recover managed standby database disconnect from session;
set lines 300
set long 999999
select * from v$managed_standby;
select max(sequence#) from v$archived_log where applied='YES';
12) start the listener of standby
13) Enable configuration; (In primary Database)
14) Start del-archive logs jobs in standby database and compr_redo in primary
15) Start zipping job in primary database
16) stop maintenance;
alter system switch logfile;
alter system switch logfile;
crontab -l
crontab -e
I - to insert
Esc and then save
:wq = to save in crontab
:q! = exit wothout save

